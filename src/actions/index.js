import pockets, { TYPES as pocketTypes } from './pockets';
import prices, { TYPES as priceTypes } from './prices';
import ui, { TYPES as uiTypes } from './ui';

export const types = {
  pockets: pocketTypes,
  prices: priceTypes,
  ui: uiTypes,
};

export default {
  pockets,
  prices,
  ui,
};
